# image-compression-svd
Python implementation of SVD-based lossy image compression using low-rank matrix approximation. Demonstrates the tradeoff between image quality and file size across multiple compression levels.
